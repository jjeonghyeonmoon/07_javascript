/*
    const 키워드는 constant 의 약자이다.
    const 는 상수라는 의미를 가진 키워드이다.
 */


// java 에서 final 키워드가 붙은 변수는 반드시 초기화를
// 선언과 동시에 해주어야 한다.
var i;
let x;

// 반드시 선언과 동시에 초기화를 해야한다.
const z = 10;
//final 키워드는 종단의 의미를 갖는다. 즉, 한 번 값을 넣으면 재할당 불가
// z = 20;

console.log(z);